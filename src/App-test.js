import React from 'react';

const SimpleTest = () => {
  return (
    <div style={{padding: '20px', backgroundColor: 'lightblue', textAlign: 'center'}}>
      <h1>TEST: If you see this, the app is updating correctly</h1>
      <p>This is a test to confirm the app is loading new code</p>
    </div>
  );
};

export default SimpleTest;